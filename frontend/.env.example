# Browser-facing config. In the compose stack these arrive as build args;
# local dev can set them in a .env before `npm start`.
REACT_APP_API_BASE_URL=http://localhost:5000
REACT_APP_VOICE_SERVICE_URL=http://localhost:8000